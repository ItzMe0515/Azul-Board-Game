# Azul Board Game - Environment Configuration
# Copy this file to .env and fill in secure values
# NEVER commit .env to version control

# Database Configuration
# Use a strong password: mix of uppercase, lowercase, numbers, symbols
# Minimum 8 characters, must contain uppercase, lowercase, and number
SA_PASSWORD=YourStrongPassword123!

# JWT Configuration
# Use a long random string (at least 32 characters)
# Generate with: openssl rand -base64 48
JWT_SECRET_KEY=YourVeryLongAndSecureJwtSecretKeyAtLeast32CharsLong!

# CORS Configuration
# Comma-separated list of allowed origins (no trailing slashes)
ALLOWED_ORIGINS=https://azul.eliminatrix.net
